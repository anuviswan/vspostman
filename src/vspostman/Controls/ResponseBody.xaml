<UserControl x:Class="VsPostman.Controls.ResponseBody"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VsPostman.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <RadioButton Style="{StaticResource {x:Type ToggleButton}}" Content="Pretty" Width="80" />
            <RadioButton Style="{StaticResource {x:Type ToggleButton}}" Content="Raw" Width="80"/>
            <RadioButton Style="{StaticResource {x:Type ToggleButton}}" Content="Review" Width="80"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock TextWrapping="Wrap" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" x:Name="responsePretty"/>
        </StackPanel>
    </Grid>
</UserControl>
